package com.iso.iso_engine.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/device-key-mappings")
public class DeviceKeyMappingController {
	
}
    private final DeviceKeyMappingService deviceKeyMappingService;

    @Autowired
    public DeviceKeyMappingController(DeviceKeyMappingService deviceKeyMappingService)

