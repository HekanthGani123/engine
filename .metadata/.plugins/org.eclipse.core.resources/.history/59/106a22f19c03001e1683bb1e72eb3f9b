package com.iso.iso_engine.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.iso.iso_engine.model.DeviceKeyMapping;

public interface DeviceKeyMappingRepository extends JpaRepository<DeviceKeyMapping, Long> {
}

